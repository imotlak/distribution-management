package com.example.distributionmanagement.presentation.ui.distribution

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.distributionmanagement.R

/**
 * Distribution Manager Dashboard Activity
 * Shows requests ready for distribution
 */
class DistributionManagerActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        // TODO: Replace with DistributionManagerScreen fragment
        // For now, display the screen
    }
}
