package com.example.distributionmanagement.presentation.ui.manager

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.distributionmanagement.R

/**
 * Manager Dashboard Activity
 * Shows pending approval requests for the manager
 */
class ManagerDashboardActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        // TODO: Replace with ManagerDashboardScreen fragment
        // For now, display the screen
    }
}
